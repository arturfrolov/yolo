# Yolo

## Installation and Dependencies

To run the project, execute the following commands:

1. Install dependencies:
   `npm install`

2. Run the project:
   `npm start`

3. For project assembly, use:
  `npm run build:dev` or `npm run build:prod`
